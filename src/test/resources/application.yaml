quarkus:
  swagger-ui:
    always-include: false
  config:
    log:
      values: true
  log:
    category: 
      "io.smallrye.config":
        level: debug
    category:
      "net.larsan.ai":
        level: debug
storage:
  default-storage:
    provider: "milvus"

embedding:
  default-model: 
    provider: "ollama"
    name: "my-model"